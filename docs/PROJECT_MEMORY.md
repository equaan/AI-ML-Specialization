# Project Memory

This is a one-image memory aid for the repository:

- [project_memory.svg](./project_memory.svg)

It captures the parts that matter most when reloading context quickly:

- multimodal input flow into the FastAPI backend
- the LangGraph-style `vision -> rag -> report` orchestration path
- report persistence and export
- the major repo areas (`backend`, `frontend`, `scripts`, `tests`, `docs`, `data`)
- the Windows demo launcher path in `scripts/start_demo_stack.ps1`

Use it as a fast re-entry point before diving back into implementation.
